models:
  - name: metricflow_time_spine
    description: "Date spine that MetricFlow uses for time-based operations."
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day